n = input()
arr=[]

for _ in range(int(n)):
    temp = input().split()
    temp = [int(x) for x in temp]
    arr.append(temp)
s1,s2 = 0,0

for i in range(int(n)):
    s1 += arr[i][i]
    s2 += arr[-i-1][i]
print (abs(s1-s2))
